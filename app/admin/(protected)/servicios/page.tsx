export default function AdminServiciosPage() {
  return (
    <section>
      <p className="text-xs uppercase tracking-widest text-muted-foreground">
        Admin
      </p>
      <h1 className="mt-4 font-serif text-4xl tracking-tight">Servicios</h1>
      <p className="mt-4 max-w-2xl text-sm leading-relaxed text-muted-foreground">
        Área reservada para el CRUD de servicios. En esta fase solo queda
        protegida y lista para conectar a Supabase Database.
      </p>
    </section>
  )
}
